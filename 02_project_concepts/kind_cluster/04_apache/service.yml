apiVersion: v1
kind: Service

metadata:
  name: apacheservice
  namespace: apachenamespace

spec:
  selector:
    app: apache
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
